{
  "name": "agentk-reddit-proxy",
  "version": "1.0.0",
  "description": "VPS proxy for Reddit API fetches — bypasses datacenter IP blocks",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-cache": "^5.1.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
